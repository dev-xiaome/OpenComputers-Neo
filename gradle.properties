# Gradle
org.gradle.jvmargs=-Xmx3G -Dfile.encoding=UTF-8
org.gradle.daemon=true
org.gradle.parallel=true
org.gradle.caching=true

# Parchment mappings
parchment_minecraft_version=1.21.1
parchment_mappings_version=2024.11.17

# Environment
minecraft_version=1.21.1
minecraft_version_range=[1.21.1]
neo_version=21.1.244
loader_version_range=[1,)

# Mod properties
mod_id=opencomputers_neo
mod_name=opencomputers_neo Neo
mod_license=MIT
mod_version=1.0.0
mod_group_id=li.cil.oc

# Scala
scala_version=2.13.14
# 移植期间只编译这些已完成的 Scala 包（逗号分隔，相对 src/main/scala 的 glob）。
# 每完成一个包就把它加进来，保证任何时刻工程都是可编译的。
scala_ported_packages=li/cil/oc/*.scala,li/cil/oc/util/**,li/cil/oc/common/Tier.scala

# 第三方库
typesafe_config_version=1.4.3
commons_codec_version=1.17.2
luaj_version=3.0.1
